# QOI-Py Requirements
# A Poorly Implemented Native Python QOI Encoder/Decoder

# Core dependencies
numpy>=1.19.0,<2.0.0
Pillow>=8.0.0,<11.0.0

# Optional dependencies for development
# pytest>=6.0.0  # For running tests (if you write any)
# black>=22.0.0  # For code formatting (if you care)
# pylint>=2.15.0  # For linting (if you're into that sort of thing)

# Note: This implementation requires Python 3.7 or higher
# Not because it uses any fancy features, but because we're not monsters
